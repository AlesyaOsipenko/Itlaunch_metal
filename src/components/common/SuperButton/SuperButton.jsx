import s from './SuperButton.module.css';


const SuperButton = () => {
    return (
        <button className={s.SuperButton}>
            Отправить
        </button>
    );
}

export default SuperButton